service: app
image: dhh/app
servers:
  - "1.1.1.1"
registry:
  username: user
  password: pw

builder:
  arch: amd64
  context: "."
  pack:
    builder: heroku/builder:24
    buildpacks:
      - heroku/ruby
